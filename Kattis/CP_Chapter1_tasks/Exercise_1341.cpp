#include <stdio.h>
#include <cmath>



int main() {
    int n; scanf("%d", &n);
    printf("%.*lf\n", n, M_PI); // Given an integer n, print pi to n digits after the decimal point
    
    return 0;
}